// noinspection JSUnusedGlobalSymbols

import { recommended } from "./configs/recommended";
import { no_literal_classnames } from "./rules";

export const configs = {
  recommended,
};

export const rules = {
  "no-literal-classnames": no_literal_classnames,
};
